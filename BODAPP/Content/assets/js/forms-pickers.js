"use strict";
$(function () {
        //var e = $("#bs-datepicker-basic"),
        //    t = $("#bs-datepicker-format"),
        //    a = $("#bs-datepicker-daterange"),
        //    i = $("#bs-datepicker-disabled-days"),
        //    r = $("#bs-datepicker-multidate"),
        //    n = $("#bs-datepicker-options"),
    var o = $(".bs-datepicker-autoclose"),
            //l = $("#bs-datepicker-inline"),
            e =
                ( o.length && o.datepicker({ todayHighlight: !0, autoclose: !0, format: "dd-mm-yyyy", orientation: isRtl ? "auto right" : "auto left" }))
            
          
            //c = document.getElementsByClassName("cancelBtn");
        //e.length && e.daterangepicker({ opens: isRtl ? "left" : "right" }),
        //    t.length && t.daterangepicker({ singleDatePicker: !0, opens: isRtl ? "left" : "right" }),
        //    a.length && a.daterangepicker({ timePicker: !0, timePickerIncrement: 30, locale: { format: "MM/DD/YYYY h:mm A" }, opens: isRtl ? "left" : "right" }),
        //    i.length &&
        //        i.daterangepicker({
        //            ranges: {
        //                Today: [moment(), moment()],
        //                Yesterday: [moment().subtract(1, "days"), moment().subtract(1, "days")],
        //                "Last 7 Days": [moment().subtract(6, "days"), moment()],
        //                "Last 30 Days": [moment().subtract(29, "days"), moment()],
        //                "This Month": [moment().startOf("month"), moment().endOf("month")],
        //                "Last Month": [moment().subtract(1, "month").startOf("month"), moment().subtract(1, "month").endOf("month")],
        //            },
        //            opens: isRtl ? "left" : "right",
        //        }),
        //    r.length && r.daterangepicker({ showWeekNumbers: !0, opens: isRtl ? "left" : "right" }),
        //    n.length && n.daterangepicker({ showDropdowns: !0, opens: isRtl ? "left" : "right" });
        //for (var s = 0; s < c.length; s++) c[s].classList.remove("btn-default"), c[s].classList.add("btn-label-primary");
        //(o = $("#timepicker-basic")), (l = $("#timepicker-min-max")), (e = $("#timepicker-disabled-times")), (t = $("#timepicker-format")), (a = $("#timepicker-step")), (i = $("#timepicker-24hours"));
        //o.length && o.timepicker({ orientation: isRtl ? "r" : "l" }),
        //    l.length && l.timepicker({ minTime: "2:00pm", maxTime: "7:00pm", showDuration: !0, orientation: isRtl ? "r" : "l" }),
        //    e.length &&
        //        e.timepicker({
        //            disableTimeRanges: [
        //                ["12am", "3am"],
        //                ["4am", "4:30am"],
        //            ],
        //            orientation: isRtl ? "r" : "l",
        //        }),
        //    t.length && t.timepicker({ timeFormat: "H:i:s", orientation: isRtl ? "r" : "l" }),
        //    a.length && a.timepicker({ step: 15, orientation: isRtl ? "r" : "l" }),
        //    i.length && i.timepicker({ show: "24:00", timeFormat: "H:i:s", orientation: isRtl ? "r" : "l" });
    });



 
